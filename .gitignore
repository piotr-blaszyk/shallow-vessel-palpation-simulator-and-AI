.DS_Store
__pycache__/
difftactile.egg-info
lr_*/
*.txt
*.mkv
*.pkl
sandbox.ipynb
*.csv
output/
lightning_logs/
experiment-capture-completed/
*.pt
saved_models/
*.msh
*.ini
*.code-workspace
*.mp4
# ...except the README demonstration videos (docker/record_videos.sh)
!videos/*.mp4
!docs/videos/*.mp4
*.avi
tac_vgnn/
*.npz
*.avi
*.zip
*.out
logs/
.vscode/
difftactile/output/
saved_models_meat/
saved_models_sim/
# One timestamped directory per seed sweep, each holding a checkpoint and its
# test-loader pickle per (configuration, seed). See cnn/seed_sweep.py.
saved_models_sweeps/
saved_models_ablation/
shallow-vessel-palpation-data.tar.gz
shallow-vessel-palpation-data.tar.gz.sha256

# Per-run output of the particle-spacing study (screenshots and logs). The
# scripts and the write-up are tracked; the captures they produce are not.
difftactile/manual_or_experimental_data/particle_spacing_study/iteration_*/
stale_artifacts_pre_new_dataset_20260815/
saved_models_legacy/**/*.pickle

# analysis/: the scripts, the figures they draw and the JSON/Markdown they write are
# tracked; the per-data-point overlay runs they can also produce are not (thousands of
# images, hundreds of MB, regenerable with ./docker/reproduce_analysis.sh --with-overlays).
analysis/overlays/
